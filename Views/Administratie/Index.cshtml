<div class="col-xl-4 col-lg-3 col-md-1 col-sm-0 col-0"></div>
<div class="col-xl-6 col-lg-8 col-md-10 col-sm-12 col-12 pb-5">

<h2>Administratie</h2>
<hr />
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        @if (User.IsInRole("Admin") || User.IsInRole("TeamRegie"))
        {
            <li class="nav-item m-1 p-1">
                <form asp-controller="Administratie" asp-action="SendEmail" asp-area="" method="post">
                    <p>Stuur uitnodiging voor registratie.</p>
                    <input class="p-1 m-1" type="text" id="InviteEmail" name="InviteEmail" placeholder="Email" class="form-control" />
                    <button type="submit" id="sendEmailButton" class="btn btn-dark">
                        Stuur uitnodiging
                    </button>
                </form>
                @* <input type="text" id="InviteEmail" name="InviteEmail" placeholder="Email" class="form-control" />
                <a class="btn btn-dark @(ViewContext.RouteData.Values["controller"].ToString() == "Administratie" ? "active" : "")"
                   href="@Url.Action("SendEmail", "Administratie", new { area = "", Email = "your@email.com" })">
                    Stuur testemail
                </a> *@

                @* <a id="sendEmailButton" class="btn btn-dark @(ViewContext.RouteData.Values["controller"].ToString() == "Administratie" ? "active" : "")"
               href="@Url.Action("SendEmail", "Administratie", new { area = "" })">Stuur uitnodiging</a> *@
        </li>
        }

        @if (User.IsInRole("Admin") || User.IsInRole("TeamRegie"))
        {
            <li class="nav-item m-1 p-1">
                <a class="btn btn-dark  @(ViewContext.RouteData.Values["controller"].ToString() == "Partner" ? "active" : "")"
               href="@Url.Action("Index", "Partner", new { area = "" })">Partners</a>
        </li>
        }

        @if (User.IsInRole("Admin") || User.IsInRole("TeamRegie") || User.IsInRole("Gebruiker"))
        {
            <li class="nav-item m-1 p-1">
                <a class="btn btn-dark  @(ViewContext.RouteData.Values["controller"].ToString() == "Experimenten" ? "active" : "")"
               href="@Url.Action("Admin", "Experimenten", new { area = "" })">Experimenten</a>
        </li>
        }

        @if (User.IsInRole("Admin") || User.IsInRole("TeamRegie"))
        {
            <li class="nav-item m-1 p-1">
                <a class="btn btn-dark  @(ViewContext.RouteData.Values["controller"].ToString() == "TeamRegies" ? "active" : "")"
               href="@Url.Action("Index", "TeamRegies", new { area = "" })">Team Regie</a>
        </li>
        }

        @if (User.IsInRole("Admin") || User.IsInRole("TeamRegie"))
        {
            <li class="nav-item m-1 p-1">
                <a class="btn btn-dark  @(ViewContext.RouteData.Values["controller"].ToString() == "User" ? "active" : "")"
               href="@Url.Action("Index", "Users", new { area = "" })">Gebruikers</a>
        </li>
        }

        @if (User.IsInRole("Admin") || User.IsInRole("TeamRegie"))
        {
            <li class="nav-item m-1 p-1">
                <a class="btn btn-dark  @(ViewContext.RouteData.Values["controller"].ToString() == "UserExperimentens" ? "active" : "")"
                   href="@Url.Action("Index", "UserExperimentens", new { area = "" })">Eigenaars van experimenten</a>
            </li>
        }
    </ul>
    </div>
<div class="col-xl-4 col-lg-3 col-md-1 col-sm-0 col-0"></div>

