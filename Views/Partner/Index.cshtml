@model IEnumerable<Klooz3.Models.Partner>

@{
    ViewData["Title"] = "Index";
}

<h1>Overzicht Partners</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.partnerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.partnerImage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.partnerLink)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.partnerDisplayOrder)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.partnerName)
            </td>
            <td>
                    <img style="width:64px" src="data:image/jpeg;base64,@Convert.ToBase64String(item.partnerImage)" alt="Partner Image" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.partnerLink)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.partnerDisplayOrder)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.partnerId">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.partnerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
